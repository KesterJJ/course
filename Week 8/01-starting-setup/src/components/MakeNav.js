function MakeNav(){
return(
    <nav>
        <a>This is an anchor tag</a>
        <p>This is a P tag. - The a and p tags are inside my "nav" component which is exported to my header component, which is exported to my root.</p>
    </nav>
);
}

export default MakeNav;